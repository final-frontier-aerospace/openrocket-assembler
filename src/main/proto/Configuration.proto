syntax = "proto3";
package com.ffaero.openrocketassembler.model.proto;

import "SupportingFile.proto";

message Configuration {
    fixed64 id = 1;
    string name = 2;
    repeated fixed64 componentIDs = 3;
    bytes templateRocketXML = 4;
    bytes templateComponentXML = 5;
    repeated SupportingFile files = 6;
}
